/**
 * OpenCode IA (geração de texto a partir de imagem via CLI no servidor).
 * Desabilitado temporariamente — consome RAM ao invocar o CLI na VPS.
 * Para reabilitar: mude para true e faça deploy.
 */
export const OPENCODE_IA_ENABLED = false;
